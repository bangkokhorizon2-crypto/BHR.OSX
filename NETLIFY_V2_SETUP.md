# Netlify V2

1. แตก ZIP นี้
2. เปิด `config.js`
3. ใส่ URL `/exec` ของ Google Apps Script V2
4. Deploy เป็น Netlify site ใหม่ ไม่ต้องแก้ site เก่า
5. ทดสอบบน iPhone/Safari

ไฟล์ `code.gs` ไม่ได้ถูก deploy ไป Netlify — ต้องนำไปใส่ใน Google Apps Script V2 ก่อน
